begin;

-- ===============
-- 1) Core tables
-- ===============

-- Users profile linked to auth.users
create table if not exists public.users (
  id uuid primary key references auth.users(id) on delete cascade,
  full_name text not null,
  phone text,
  role text not null default 'Parent' check (role in ('Admin','Teacher','Accountant','Parent')),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Grades
create table if not exists public.grades (
  id bigint generated by default as identity primary key,
  name text not null unique check (name in ('Mầm','Chồi','Lá')),
  sort_order int not null default 0,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Classes
create table if not exists public.classes (
  id bigint generated by default as identity primary key,
  grade_id bigint not null references public.grades(id) on delete restrict,
  name text not null,
  teacher_id uuid references public.users(id) on delete set null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (grade_id, name)
);

-- Students
create table if not exists public.students (
  id uuid primary key default gen_random_uuid(),
  class_id bigint not null references public.classes(id) on delete cascade,
  student_code text not null unique,
  full_name text not null,
  date_of_birth date,
  gender text check (gender in ('Male','Female','Other')),
  health_info jsonb not null default '{}'::jsonb,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Parents
create table if not exists public.parents (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references public.users(id) on delete set null,
  full_name text not null,
  phone text,
  email text,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Link table: student <-> parent
create table if not exists public.student_parent (
  student_id uuid not null references public.students(id) on delete cascade,
  parent_id uuid not null references public.parents(id) on delete cascade,
  relation_type text not null default 'Guardian',
  created_at timestamptz not null default now(),
  primary key (student_id, parent_id),
  unique (student_id, parent_id)
);

-- Attendance
create table if not exists public.attendance (
  id uuid primary key default gen_random_uuid(),
  student_id uuid not null references public.students(id) on delete cascade,
  class_id bigint not null references public.classes(id) on delete cascade,
  attendance_date date not null,
  status text not null check (status in ('present','absent')),
  note text,
  created_by uuid references public.users(id) on delete set null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (student_id, attendance_date)
);

-- Fee types
create table if not exists public.fee_types (
  id bigint generated by default as identity primary key,
  name text not null unique,
  amount_vnd numeric(12,0) not null check (amount_vnd > 0),
  is_active boolean not null default true,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Fee records
create table if not exists public.fee_records (
  id uuid primary key default gen_random_uuid(),
  student_id uuid not null references public.students(id) on delete cascade,
  class_id bigint not null references public.classes(id) on delete cascade,
  fee_type_id bigint not null references public.fee_types(id) on delete restrict,
  school_year text not null,
  amount_vnd numeric(12,0) not null check (amount_vnd > 0),
  due_date date,
  paid_amount_vnd numeric(12,0) not null default 0 check (paid_amount_vnd >= 0),
  status text not null default 'unpaid' check (status in ('unpaid','partial','paid')),
  created_by uuid references public.users(id) on delete set null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (student_id, fee_type_id, school_year)
);

-- Notifications
create table if not exists public.notifications (
  id uuid primary key default gen_random_uuid(),
  recipient_parent_id uuid references public.parents(id) on delete cascade,
  recipient_user_id uuid references public.users(id) on delete cascade,
  title text not null,
  body text not null,
  kind text not null default 'absence' check (kind in ('absence','general')),
  student_id uuid references public.students(id) on delete set null,
  attendance_id uuid references public.attendance(id) on delete set null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);

-- Notification reads
create table if not exists public.notification_reads (
  id uuid primary key default gen_random_uuid(),
  notification_id uuid not null references public.notifications(id) on delete cascade,
  parent_id uuid not null references public.parents(id) on delete cascade,
  user_id uuid references public.users(id) on delete set null,
  read_at timestamptz not null default now(),
  unique (notification_id, parent_id)
);

-- School settings
create table if not exists public.school_settings (
  id bigint generated by default as identity primary key,
  school_name text not null,
  address text,
  school_year text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (school_year)
);

-- ==================
-- 2) Indexes
-- ==================

create index if not exists idx_students_class_id on public.students(class_id);
create index if not exists idx_attendance_student_date on public.attendance(student_id, attendance_date);
create index if not exists idx_fee_records_student on public.fee_records(student_id);

-- Also add some helpful FK indexes
create index if not exists idx_classes_grade_id on public.classes(grade_id);
create index if not exists idx_students_student_code on public.students(student_code);
create index if not exists idx_student_parent_parent_id on public.student_parent(parent_id);
create index if not exists idx_attendance_class_id on public.attendance(class_id, attendance_date);
create index if not exists idx_fee_records_fee_type_id on public.fee_records(fee_type_id);
create index if not exists idx_fee_records_class_id on public.fee_records(class_id);
create index if not exists idx_notifications_recipient_parent_id on public.notifications(recipient_parent_id);
create index if not exists idx_notifications_recipient_user_id on public.notifications(recipient_user_id);

-- ==================
-- 3) updated_at trigger
-- ==================

create or replace function public.set_updated_at()
returns trigger language plpgsql as $$
begin
  new.updated_at = now();
  return new;
end;
$$;

-- Attach to each table
do $$
begin
  if not exists (
    select 1 from pg_trigger where tgname = 'trg_users_set_updated_at'
  ) then
    create trigger trg_users_set_updated_at before update on public.users
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_grades_set_updated_at') then
    create trigger trg_grades_set_updated_at before update on public.grades
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_classes_set_updated_at') then
    create trigger trg_classes_set_updated_at before update on public.classes
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_students_set_updated_at') then
    create trigger trg_students_set_updated_at before update on public.students
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_parents_set_updated_at') then
    create trigger trg_parents_set_updated_at before update on public.parents
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_student_parent_set_updated_at') then
    create trigger trg_student_parent_set_updated_at before update on public.student_parent
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_attendance_set_updated_at') then
    create trigger trg_attendance_set_updated_at before update on public.attendance
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_fee_types_set_updated_at') then
    create trigger trg_fee_types_set_updated_at before update on public.fee_types
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_fee_records_set_updated_at') then
    create trigger trg_fee_records_set_updated_at before update on public.fee_records
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_notifications_set_updated_at') then
    create trigger trg_notifications_set_updated_at before update on public.notifications
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_notification_reads_set_updated_at') then
    -- notification_reads has read_at; still keep updated_at pattern if needed
    create trigger trg_notification_reads_set_updated_at before update on public.notification_reads
    for each row execute function public.set_updated_at();
  end if;

  if not exists (select 1 from pg_trigger where tgname = 'trg_school_settings_set_updated_at') then
    create trigger trg_school_settings_set_updated_at before update on public.school_settings
    for each row execute function public.set_updated_at();
  end if;
end $$;

-- ==================
-- 4) Auto-create user record on auth signup
-- ==================

create or replace function public.handle_new_user()
returns trigger language plpgsql security definer as $$
begin
  -- Avoid overriding existing profile
  insert into public.users (id, full_name, phone, role)
  values (
    new.id,
    coalesce(new.raw_user_meta_data->>'full_name', new.raw_user_meta_data->>'name', 'User'),
    new.raw_user_meta_data->>'phone',
    coalesce(new.raw_user_meta_data->>'role', 'Parent')
  )
  on conflict (id) do nothing;

  return new;
end;
$$;

-- Attach to auth.users
drop trigger if exists on_auth_user_created on auth.users;
create trigger on_auth_user_created
  after insert on auth.users
  for each row execute function public.handle_new_user();

-- ==================
-- 5) RLS policies
-- ==================

-- Helper: role from users.role if present; otherwise JWT claim
-- (We map using public.users.role for Parent ownership checks)

alter table public.users enable row level security;
alter table public.grades enable row level security;
alter table public.classes enable row level security;
alter table public.students enable row level security;
alter table public.parents enable row level security;
alter table public.student_parent enable row level security;
alter table public.attendance enable row level security;
alter table public.fee_types enable row level security;
alter table public.fee_records enable row level security;
alter table public.notifications enable row level security;
alter table public.notification_reads enable row level security;
alter table public.school_settings enable row level security;

-- USERS
create policy "admin_all_users" on public.users for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "self_read_users" on public.users for select to authenticated using (id = auth.uid());

-- GRADES
create policy "admin_crud_grades" on public.grades for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

-- CLASSES
create policy "admin_crud_classes" on public.classes for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "teacher_crud_classes" on public.classes for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
));

-- STUDENTS
create policy "admin_crud_students" on public.students for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "teacher_crud_students" on public.students for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
));

-- Parents ownership: parent can read students linked to them
create policy "parent_read_students" on public.students for select to authenticated using (
  exists (
    select 1
    from public.parents p
    join public.student_parent sp on sp.parent_id = p.id
    where p.user_id = auth.uid()
      and sp.student_id = students.id
  )
);

-- PARENTS
create policy "admin_crud_parents" on public.parents for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "parent_read_own_profile" on public.parents for select to authenticated using (user_id = auth.uid());

-- STUDENT_PARENT (link table)
create policy "admin_crud_student_parent" on public.student_parent for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "teacher_crud_student_parent" on public.student_parent for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
));

-- ATTENDANCE
create policy "admin_crud_attendance" on public.attendance for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "teacher_crud_attendance" on public.attendance for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Teacher'
));

create policy "parent_read_own_children_attendance" on public.attendance for select to authenticated using (
  exists (
    select 1
    from public.parents p
    join public.student_parent sp on sp.parent_id = p.id
    where p.user_id = auth.uid()
      and sp.student_id = attendance.student_id
  )
);

-- FEE_TYPES (mostly read by roles)
create policy "admin_crud_fee_types" on public.fee_types for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "teacher_read_fee_types" on public.fee_types for select to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role in ('Admin','Teacher','Accountant')
));

-- FEE_RECORDS
create policy "admin_crud_fee_records" on public.fee_records for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "accountant_crud_fee_records" on public.fee_records for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Accountant'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Accountant'
));

-- Parent can read their children's fee records
create policy "parent_read_fee_records" on public.fee_records for select to authenticated using (
  exists (
    select 1
    from public.parents p
    join public.student_parent sp on sp.parent_id = p.id
    where p.user_id = auth.uid()
      and sp.student_id = fee_records.student_id
  )
);

-- NOTIFICATIONS
create policy "admin_crud_notifications" on public.notifications for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

create policy "parent_read_notifications" on public.notifications for select to authenticated using (
  recipient_user_id = auth.uid()
  or exists (
    select 1 from public.parents p
    where p.user_id = auth.uid() and p.id = notifications.recipient_parent_id
  )
);

create policy "parent_create_reads" on public.notification_reads for insert to authenticated with check (
  user_id = auth.uid() or exists (select 1 from public.parents p where p.user_id = auth.uid() and p.id = notification_reads.parent_id)
);

create policy "parent_read_own_notification_reads" on public.notification_reads for select to authenticated using (
  user_id = auth.uid() or exists (select 1 from public.parents p where p.user_id = auth.uid() and p.id = notification_reads.parent_id)
);

-- SCHOOL SETTINGS
create policy "admin_crud_school_settings" on public.school_settings for all to authenticated using (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
)) with check (exists (
  select 1 from public.users u2 where u2.id = auth.uid() and u2.role = 'Admin'
));

-- ==================
-- 6) Trigger: Send notification when student is absent
-- ==================

create or replace function public.notify_absence()
returns trigger language plpgsql as $$
declare
  v_student_id uuid;
  v_attendance_id uuid;
begin
  if (new.status = 'absent') and (tg_op = 'INSERT' or new.status <> old.status) then
    v_student_id := new.student_id;
    v_attendance_id := new.id;

    -- Insert a notification for each parent of the student
    insert into public.notifications (recipient_parent_id, recipient_user_id, title, body, kind, student_id, attendance_id)
    select
      p.id as recipient_parent_id,
      p.user_id as recipient_user_id,
      'Vắng mặt' as title,
      'Hôm nay ' || coalesce(s.full_name,'bé') || ' đã vắng mặt ngày ' || new.attendance_date::text || '.' as body,
      'absence' as kind,
      s.id as student_id,
      new.id as attendance_id
    from public.student_parent sp
    join public.parents p on p.id = sp.parent_id
    join public.students s on s.id = sp.student_id
    where sp.student_id = v_student_id;
  end if;

  return new;
end;
$$;

drop trigger if exists trg_notify_absence on public.attendance;
create trigger trg_notify_absence
  after insert or update of status on public.attendance
  for each row execute function public.notify_absence();

-- ==================
-- 7) Grants (Data API / SQL access)
-- ==================

-- Grant base privileges so RLS can take effect
grant usage on schema public to authenticated;

-- For simplicity, grant authenticated CRUD where relevant; RLS will filter.
-- users
grant select on public.users to authenticated;
-- allow teachers/admin/accountant to manage via RLS (still needs grants)
grant select, insert, update, delete on public.grades to authenticated;
grant select, insert, update, delete on public.classes to authenticated;
grant select, insert, update, delete on public.students to authenticated;
grant select, insert, update, delete on public.parents to authenticated;
grant select, insert, update, delete on public.student_parent to authenticated;
grant select, insert, update, delete on public.attendance to authenticated;
grant select, insert, update, delete on public.fee_types to authenticated;
grant select, insert, update, delete on public.fee_records to authenticated;
grant select, insert, update, delete on public.notifications to authenticated;
grant select, insert, update, delete on public.notification_reads to authenticated;
grant select, insert, update, delete on public.school_settings to authenticated;

commit;
